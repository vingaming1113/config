conky.config = {
    own_window = true,
    own_window_type = 'desktop',    -- Makes Conky act like part of the wallpaper
    own_window_class = 'Conky',     -- Explicitly set window class
    own_window_transparent = true,  -- Keeps Conky transparent
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    double_buffer = true,           -- Avoids flickering
    alignment = 'top_right',        -- Positioning
    gap_x = 10,                     -- Horizontal position
    gap_y = 50,                     -- Move it a bit down from the top
    minimum_width = 200,
    minimum_height = 100,           -- Adjust height to fit content
    draw_shades = false,
    draw_outline = false,
    draw_borders = false,
    draw_graph_borders = false,
    use_xft = true,
    font = 'DejaVu Sans Mono:size=12',  -- Increased font size for better readability
    own_window_argb_visual = true,
    own_window_argb_value = 150,
    border_width = 0,               -- No border
    border_inner_margin = 0,
    border_outer_margin = 0,
};

conky.text = [[
${font DejaVu Sans Mono:size=14}${color1}${time %Y-%m-%d %H:%M:%S}${color}
${font DejaVu Sans Mono:size=12}${color2}CPU:${color} ${cpu}%
${color2}RAM:${color} $mem/$memmax
${color2}Uptime:${color} ${uptime_short}
]];
